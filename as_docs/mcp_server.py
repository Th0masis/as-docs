"""Placeholder MCP server — Phase 3 implementation."""


def start_server(config, use_http: bool = False, port: int = 8765) -> None:
    raise NotImplementedError(
        "MCP server is not yet implemented (Phase 3). "
        "Generate docs first with 'as-docs generate'."
    )
